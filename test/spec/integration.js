define(['models/slide-model', 'chai', 'mocha'], function(SlideModel, chai) {
  var tests = function() {

    assert = chai.assert;

    describe('SlideCollection', function() {
      describe('Creation', function() {
      });
    });

  };

  return tests
});
